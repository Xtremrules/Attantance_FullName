@using Attantance_FullName.Models;
@model IEnumerable<Attantance_FullName.Models.Log>

@{
    ViewBag.Title = "logs";
}

<h2>logs</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AppUserID)
        </th>
        <th>
            @Html.DisplayName("Full Name")
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LoginTime)
        </th>
        @*<th></th>*@
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.AppUserID)
            </td>
            <td>
                @Html.FullName_(item.AppUserID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LoginTime)
            </td>
            @*<td>
                    @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
                    @Html.ActionLink("Details", "Details", new { id=item.ID }) |
                    @Html.ActionLink("Delete", "Delete", new { id=item.ID })
                </td>*@
        </tr>
    }

</table>
